function ht_progress_bar_js() {
    ht_progress_bar(), 
    jQuery(document).trigger("ht_progress_bar_js")
}
"function" != typeof window.ht_progress_bar && (window.ht_progress_bar = function() {
        "undefined" != typeof jQuery.fn.waypoint && jQuery(".ht_progress_bar").waypoint(function() {
            jQuery(this).find(".ht_single_bar").each(function(index) {
                var $this = jQuery(this),
                    bar = $this.find(".ht_prog_bar"),
                    val = bar.data("percentage-value");
                setTimeout(function() {
                    bar.css({
                        width: val + "%"
                    })
                }, 200 * index)
            })
        }, {
            offset: "85%"
        })
    }), 
    "function" != typeof jQuery(document).ready(function($) {
    window.ht_progress_bar_js()
});